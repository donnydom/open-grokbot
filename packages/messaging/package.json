{
  "name": "@open-grokbot/messaging",
  "version": "0.1.0",
  "description": "Multi-agent messaging: agent-to-agent private messages, group chat orchestration, broadcast fan-out and subagent runtime.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {
    "@open-grokbot/core": "0.1.0",
    "@open-grokbot/state": "0.1.0"
  }
}
