{
  "name": "@open-grokbot/transport",
  "version": "0.1.0",
  "description": "Transport layer: MessagePort frame protocol (port server/client) and the HTTP+SSE gateway wire (client and server), mirroring the coordinator of the original platform.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {
    "@open-grokbot/core": "0.1.0"
  }
}
